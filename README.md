When the blocks in ipynb files are executed, a folder named "data" will be created in the current directory containing the dataset. Then you can continue without any problems.

DATASETS:
1- CIFAR-10
2- STL-10
